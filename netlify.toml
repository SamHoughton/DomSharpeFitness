[build]
  publish = "."

# The site is plain HTML/CSS/JS, nothing to compile.
[build.processing]
  skip_processing = false

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=(), payment=(), interest-cohort=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"
    # Sources allowed: Google Fonts, Font Awesome (Cloudflare), the Google Maps
    # embed, Google Analytics (only fires once a Measurement ID is set), and the
    # Railway API the consultation form posts to.
    Content-Security-Policy = """
      default-src 'self';
      script-src 'self' 'unsafe-inline' https://www.googletagmanager.com;
      style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com;
      font-src 'self' https://fonts.gstatic.com https://cdnjs.cloudflare.com;
      img-src 'self' data: https://www.googletagmanager.com https://*.google-analytics.com https://maps.gstatic.com https://*.googleapis.com;
      frame-src https://maps.google.com https://www.google.com;
      connect-src 'self' https://domsharpefitness-production.up.railway.app https://*.google-analytics.com;
      form-action 'self';
      base-uri 'self';
      object-src 'none';
      frame-ancestors 'self'
    """

# Long-cache fingerprint-free assets conservatively; HTML stays fresh.
[[headers]]
  for = "/img/*"
  [headers.values]
    Cache-Control = "public, max-age=604800"

[[headers]]
  for = "/admin.html"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"
